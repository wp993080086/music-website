export default {
	// 设置用户信息
	setUserMsg(state, val) {
		state.userMsg = val
	},
	// 设置Cookie
	setCookie(state, val) {
		state.cookie = val
	},
	// 设置Token
	setToken(state, val) {
		state.token = val
	}
}
